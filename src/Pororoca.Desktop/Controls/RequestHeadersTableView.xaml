<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:Pororoca.Desktop.ViewModels.DataGrids;assembly=Pororoca.Desktop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:Pororoca.Desktop.Controls;assembly=Pororoca.Desktop"
    mc:Ignorable="d"
    d:DesignWidth="1350"
    d:DesignHeight="700"
    x:Class="Pororoca.Desktop.Controls.RequestHeadersTableView"
    x:CompileBindings="True"
    x:DataType="vm:RequestHeadersDataGridViewModel">

  <DataGrid
      Name="datagrid"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      AutoGenerateColumns="False"
      ItemsSource="{Binding Items}"
      SelectionChanged="OnSelectedRequestHeadersChanged"
      CanUserResizeColumns="True"
      HeadersVisibility="All"
      Classes="DragAndDrop RequestHeadersDragAndDrop">
    <!--DataGrid.KeyBindings>
        <KeyBinding Command="{Binding RequestHeadersTableVm.CutCmd, Mode=OneWay}" Gesture="Ctrl+X" />
        <KeyBinding Command="{Binding RequestHeadersTableVm.CopyCmd, Mode=OneWay}" Gesture="Ctrl+C" />
        <KeyBinding Command="{Binding RequestHeadersTableVm.PasteCmd, Mode=OneWay}" Gesture="Ctrl+V" />
        <KeyBinding Command="{Binding RequestHeadersTableVm.DuplicateCmd, Mode=OneWay}" Gesture="Ctrl+D" />
        <KeyBinding Command="{Binding RequestHeadersTableVm.DeleteCmd, Mode=OneWay}" Gesture="Delete" />
      </DataGrid.KeyBindings-->
    <DataGrid.ContextMenu>
      <ContextMenu>
        <MenuItem
            Header="{Binding i18n.ContextMenu.Cut, Mode=OneWay}"
            Command="{Binding CutCmd, Mode=OneWay}"
            InputGesture="Ctrl+X"/>
        <MenuItem
            Header="{Binding i18n.ContextMenu.Copy, Mode=OneWay}"
            Command="{Binding CopyCmd, Mode=OneWay}"
            InputGesture="Ctrl+C"/>
        <MenuItem
            Header="{Binding i18n.ContextMenu.Paste, Mode=OneWay}"
            Command="{Binding PasteCmd, Mode=OneWay}"
            InputGesture="Ctrl+V"
            IsEnabled="{Binding InnerClipboardArea.CanPaste, Mode=OneWay}"/>
        <MenuItem
            Header="{Binding i18n.ContextMenu.Delete, Mode=OneWay}"
            Command="{Binding DeleteCmd, Mode=OneWay}"
            InputGesture="Delete"/>
      </ContextMenu>
    </DataGrid.ContextMenu>
    <DataGrid.Columns>
      <DataGridCheckBoxColumn
          Width="0.24*"
          Binding="{Binding Enabled}">
        <DataGridCheckBoxColumn.Header>
          <TextBlock Text="{Binding i18n.HttpRequest.HeaderEnabled, Mode=OneWay}"/>
        </DataGridCheckBoxColumn.Header>
      </DataGridCheckBoxColumn>
      <DataGridTemplateColumn Width="0.32*">
        <DataGridTemplateColumn.Header>
          <TextBlock Text="{Binding i18n.HttpRequest.HeaderName, Mode=OneWay}"/>
        </DataGridTemplateColumn.Header>
        <DataGridTemplateColumn.CellTemplate>
          <DataTemplate>
            <TextBlock
                Text="{Binding Key, Mode=OneWay}"
                VerticalAlignment="Center"
                Padding="12,0,12,0"/>
          </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
        <DataGridTemplateColumn.CellEditingTemplate>
          <DataTemplate>
            <controls:HeaderNameTextBox
                Text="{Binding Key}"
                VerticalContentAlignment="Center"
                Padding="12,0,12,0"/>
          </DataTemplate>
        </DataGridTemplateColumn.CellEditingTemplate>
      </DataGridTemplateColumn>
      <DataGridTextColumn
          Width="0.32*"
          Binding="{Binding Value}">
        <DataGridTextColumn.Header>
          <TextBlock Text="{Binding i18n.HttpRequest.HeaderValue, Mode=OneWay}"/>
        </DataGridTextColumn.Header>
      </DataGridTextColumn>
      <DataGridTemplateColumn
          Width="0.12*"
          Header="">
        <DataGridTemplateColumn.CellTemplate>
          <DataTemplate>
            <Button
                Command="{Binding RemoveParamCmd, Mode=OneWay}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                Classes="Trash">
            </Button>
          </DataTemplate>
        </DataGridTemplateColumn.CellTemplate>
      </DataGridTemplateColumn>
    </DataGrid.Columns>
  </DataGrid>

</UserControl>